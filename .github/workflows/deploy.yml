name: Deploy to server

on: push

jobs:
    first_jobs:
        runs-on: ubuntu-latest

        env:
            MY_ENV_VAR: "Hola mundo"
        steps:
            - name: "Hola mundo"
              run: echo "Hola mundo desde el primer job ${MY_ENV_VAR}"
    second_job:
        if: ${{ github.ref == 'refs/heads/main' }}
        needs: first_jobs
        runs-on: ubuntu-latest

        steps:
            - name: "Hola mundo"
              run: echo "Hola mundo desde el segundo job 2"